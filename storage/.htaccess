# Deny all direct HTTP access to the storage directory.
# Log files, audit trails, and temp data must never be served to browsers.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
